# AGENTS.md — 코드를 쓰기 전에

이 파일은 사람과 AI 코딩 에이전트 모두를 위한 것이다. **작업을 시작하기 전에 이 문서를 끝까지 읽는다.**

kplacelab은 공공기관을 대상으로 수치를 공표하는 제품이다. 잘못된 숫자 하나가 정정 요청과 신뢰 붕괴로 이어지고, 한번 붕괴하면 회복되지 않는다. 그래서 이 저장소에는 **성능이나 우아함보다 우선하는 규칙**이 있다.

---

## 1. 절대 불변식

아래를 위반하는 코드는 리뷰에서 무조건 반려한다. 편의를 위한 예외도 없다.

### INV-1 · 두 모집단을 합산하지 않는다

`units.population` 은 `local_gov`(자치단체 243) 과 `special_zone`(특별구역) 두 값을 갖는다. 이 둘은 **근거 법률과 조직 성격이 다른 별개의 전수**다.

- 두 모집단을 `UNION` 하는 뷰·쿼리·집계를 만들지 않는다.
- `"전국 249곳"` 같은 합산 문자열을 만들지 않는다.
- 모든 집계는 `group by population` 이거나, 모집단이 `where` 로 고정되어 있어야 한다.

집계 함수는 반드시 모집단을 인자로 받는다. 기본값을 두지 않는다.

```ts
// 금지
getCoverage()
// 필수
getCoverage({ population: 'local_gov' })
```

### INV-2 · 판정은 4분이며, "판정 불가"를 뭉뚱그리지 않는다

`robots_verdict` 은 `open` / `blocked_all` / `blocked_selective` / `no_file` / `undetermined` 이다.

- `undetermined` 를 `blocked_*` 로 합치지 않는다 → 서버 장애를 정책적 차단으로 오도한다.
- `undetermined` 를 `open` 으로 합치지 않는다 → 수집되지 않는 곳을 정상으로 보고한다.
- 공개 집계 문장은 **네 값을 모두** 낸다.
- `undetermined` 는 항상 `undetermined_reason` 을 동반한다 (DB CHECK 제약으로 강제됨).

### INV-3 · 점수로 정렬하지 않는다

순위표를 만들지 않는 것은 비목표(NG-1)다. 코드 수준에서도 막는다.

- 단위 목록 API는 `order` 파라미터에 점수·측정값 계열 컬럼을 **거부**한다 (허용 목록 방식).
- UI에 "1위/최하위/랭킹/베스트/워스트" 문자열을 넣지 않는다.
- 정렬 기본값은 **행정구역 코드순**이다.

### INV-4 · 자발적 표본으로 전국을 말하지 않는다

Layer 2 데이터를 표시하는 모든 화면·API·리포트는 분모를 **참여한 단위 수**로 쓴다.

- 문구는 `참여한 N곳 중 …` 이다. `전국 N곳 중 …` 이 아니다.
- Layer 1 값과 Layer 2 값을 같은 표·같은 차트 축에 넣지 않는다.
- Layer 2와 Layer 3도 같은 표에 넣지 않는다 (측정 조건이 다르다).

타입으로 강제한다. `Layer1Stat` 과 `Layer2Stat` 은 서로 호환되지 않는 별개 타입이며, 하나의 컴포넌트가 둘을 동시에 받지 않는다.

### INV-5 · 차단을 우회하지 않는다

- User-Agent 를 위장하지 않는다. 상수 `SCANNER_UA` 하나만 쓴다.
- `robots.txt` 가 우리 수집기를 거부하면 **그 사실만 기록하고 더 이상 요청하지 않는다.**
- 프록시 회전, IP 변경, 캡차 우회, 헤드리스 브라우저로의 우회 — 전부 금지.
- 도메인당 주 1회, 요청 간 최소 `SCAN_MIN_INTERVAL_MS` 를 지킨다.

우회하는 순간 이 제품의 정당성이 사라진다. 이것은 기술 선택이 아니라 존립 조건이다.

### INV-6 · AI 응답 원문을 공개 경로로 내보내지 않는다

- `observations` 에 원문 전문을 저장하지 않는다. 판정 결과와 최소 인용구까지다.
- 공개 API·페이지·OG 이미지·내보내기에서 원문 필드를 노출하지 않는다.
- 이유는 약관이 아니라 **편집 책임**이다. 오답 원문을 우리가 확산시키는 모양을 만들지 않는다.

### INV-7 · 측정 조건 없는 관측은 존재할 수 없다

`observations` 행은 `method_version`, `ai_service`, `web_search`, `language`, `measured_on` 이 모두 있어야 한다. NOT NULL 이며, 기본값으로 채우지 않는다.

`method_version` 이 다른 관측은 **기본적으로 비교하지 않는다.** 비교 쿼리는 버전을 명시적으로 고정하거나, 환산 계수를 거친다.

### INV-8 · 공표 전 판정은 승격을 거친다

주간 스캔 결과(`tech_scans`)는 관측이지 판정이 아니다. **2주 연속 동일 결과**일 때만 `verdicts` 로 승격되고, 승격된 것만 공개된다. 단발 스캔 결과를 공개 화면에 직접 붙이지 않는다.

### INV-9 · 집계는 저장하지 않는다 — 그리고 평균만 내지 않는다

`item_results` 는 **반복 회차별로** 저장한다(`rep` 컬럼). 집계는 조회 시점에 한다.

- 문항당 1행으로 뭉치지 않는다. **집계된 값에서 분포를 복원할 방법은 없다.**
- 정확도 비율을 표시하는 모든 화면·리포트는 **Floor Risk를 함께** 낸다. 평균만 내보내는 응답 객체를 만들지 않는다.
- `부재`와 `작화`를 같은 무게로 평균하지 않는다. 작화는 `floor_risk = critical` 이다.

→ [ADR-0006](./docs/adr/0006-distribution-over-score.md)

### INV-10 · 강건성 검사를 통과하지 않은 문항의 결과를 공표하지 않는다

문항은 콘텐츠가 아니라 **측정 도구**다.

- `question_robustness.sensitive = true` 인 문항은 공개 집계에서 제외한다.
- `question_robustness` 행이 **없는** 문항도 제외한다 — 검사하지 않은 것은 통과한 것이 아니다.
- 문항은 8블록으로 저장하고, 프롬프트는 블록에서 조립한다. 자연어 프롬프트를 하드코딩하지 않는다.

→ [ADR-0007](./docs/adr/0007-questions-are-instruments.md)

### INV-11 · 사전 등록 없는 측정 결과를 공표하지 않는다

모든 측정은 시작 전에 **가설·방법·판정 기준·반증 조건·중단 규칙·공표 약속**을 공개한다.

- 사전 등록 없이 수행한 관측은 **탐색적**으로만 쓰고, 화면·리포트에 그렇게 표시한다.
- **결과를 본 뒤 반증 조건을 바꾸지 않는다.** 바꿔야 하면 새 사전 등록을 쓰고 이전 것을 이력으로 남긴다.
- 반증 조건은 `docs/EVIDENCE.md` 의 해당 주장과 연결한다.

→ [ADR-0009](./docs/adr/0009-preregistration.md)

### INV-12 · 대장에 없는 주장을 쓰지 않는다

공개 문서·영업 자료·발표의 모든 사실 주장은 `docs/EVIDENCE.md` 에 등록되어 있어야 하고, **그 상태를 그대로 표기**한다.

- `미검증` 주장을 확정처럼 말하지 않는다.
- UI 문구·리포트 템플릿에 하드코딩된 사실 주장을 넣지 않는다. 대장을 참조한다.
- 새 주장을 쓰기 전에 대장에 먼저 등록한다.

---

## 2. 작업 순서

1. `docs/PRD.md` 에서 해당 FR 번호를 찾는다. 없으면 **먼저 PRD에 추가**하고, 왜 필요한지 쓴다.
2. `docs/knowledge/` 에서 관련 지식 파일을 읽는다. 판정 기준·문항·법령 근거는 거기가 원본이다.
3. 스키마를 바꾼다면 `supabase/migrations/` 에 새 마이그레이션을 만든다. **기존 마이그레이션을 수정하지 않는다.**
4. 되돌리기 어려운 결정이면 `docs/adr/` 에 ADR을 먼저 쓴다.
5. 구현한다.
6. 불변식 테스트를 돌린다: `pnpm test:invariants`

---

## 3. 지식과 코드의 경계

**지식 파일이 원본이고, 코드는 그것을 따른다.**

| 이런 것은 | 여기에 있다 | 코드에서는 |
|---|---|---|
| 질문 12 + 8 문항 원문 | `K04-question-sets.md` → `supabase/seed/questions.csv` | DB에서 읽는다. 하드코딩 금지 |
| 판정 기준·경계값 | `K03-measurement-spec.md` | 상수 파일 하나에 모으고 파일 상단에 출처 주석 |
| 단위 마스터 | `K02-units-taxonomy.md` → `supabase/seed/units.csv` | DB에서 읽는다 |
| 법령·제도 근거 | `K05-legal-policy.md` | UI 문구에서 참조. 코드에 사실을 적지 않는다 |
| 공표 문안 템플릿 | `K07-publication-policy.md` | i18n 리소스로 옮기고 출처 주석 |

문항이나 기준을 바꾸고 싶으면 **지식 파일과 `method_version` 을 함께 올린다.** 코드만 고치면 과거 데이터와의 비교 가능성이 조용히 깨진다.

---

## 4. 문구 규칙

이 제품은 문장으로 신뢰를 잃는다. 아래는 코드 리뷰 대상이다.

**쓰지 않는 말**

- 순위 / 랭킹 / 등급 / 1위 / 최하위 / 베스트 / 워스트
- 노출 보장 / 상위 노출 / AI 최적화 보장
- 전국 평균 (Layer 2 데이터에 대해)
- 차단당했다 (판정 불가인 경우)
- 정확도 N% (Floor Risk 없이 단독으로)
- **세계 최초 · 세계 유일 · 국내 유일 · 독보적 · 특허받은 방법론** — 선행이 실재하고, 없음은 증명할 수 없다 (`K11` §6)
- **특정 지자체를 나쁜 사례로 지목하는 문구** — 전국은 익명, 개별은 좋은 사례만 (`K13` §3)

**쓰는 말**

- `개방 N · 차단 N · 파일 없음 N · 판정 불가 N`
- `참여한 N곳 중 …`
- `2026-09-05 기준, 방법론 v1.0 으로 측정`
- `이 값은 소관 시·도 도메인의 값입니다` (B형 단위에 필수 병기)

**설명하는 말**

측정값을 보여줄 때는 **무엇이 막고 있는지는 이 측정으로 알 수 없다**는 사실을 함께 쓴다. 원인 규명은 Layer 4의 일이고, 무료 화면이 원인과 조치 방법을 말하면 그것은 정직하지 않다.

---

## 5. 데이터를 다룰 때

- **개인정보**: 자유 질문에 개인정보가 섞여 들어온다고 가정한다. 검수 통과 전에는 어떤 공개 경로로도 나가지 않는다. `questions.approved` 가 기본 `false` 인 이유다.
- **제출자**: 이메일은 알림·정정 목적으로만 보관하고 소속 증명으로 쓰지 않는다.
- **원문 보존**: `tech_scans` 는 원문 전체 대신 해시와 요약을 남긴다. 분쟁 시 재현 가능성을 위해 `raw_hash` 는 반드시 채운다.
- **삭제**: 단위 마스터 행을 지우지 않는다. `active=false` 로 내린다. 통폐합·명칭 변경은 이력이다.

---

## 6. 커밋과 PR

```
feat(scanner): robots 4분 판정 + 사유 코드 구현
fix(api): 단위 목록 정렬 허용 목록 누락 (INV-3)
docs(knowledge): K03 판정 경계값 v1.1
chore(db): observations 지명 합계 트리거
```

PR 본문에는 다음을 쓴다.

- 관련 FR 번호 (없으면 PRD 갱신 링크)
- 건드린 불변식이 있으면 어느 것이고 왜 안전한지
- 스키마 변경이면 마이그레이션 파일명과 되돌리는 법

---

## 7. 이 제품이 스스로 지켜야 할 것

우리는 남의 누리집의 AI 접근성을 측정해 공표한다. **우리 자신의 사이트가 그 기준을 통과하지 못하면 이 제품은 우스워진다.**

- `kplacelab` 의 `robots.txt` 는 개방이고, `Sitemap:` 을 선언한다.
- 모든 공개 페이지에 유효한 JSON-LD 를 넣는다.
- 방법론 페이지와 인용 가이드는 로그인 없이 읽힌다.
- 우리 사이트도 우리 스캐너의 대상에 포함하고, 결과를 공개한다.

이것은 농담이 아니라 CI 체크 항목이다.
